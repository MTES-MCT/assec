import React from 'react';

const MockupHeader = () => (
  <div id="main-header">
    <h1 className="align-center">
      <span>ASSEC</span>
    </h1>
  </div>
);

MockupHeader.propTypes = {};

export default MockupHeader;
