import React from 'react';
import PropTypes from 'prop-types';

const ChoiceInput = () => <div />;
ChoiceInput.propTypes = {
  choice: PropTypes.object.isRequired,
  provider: PropTypes.array.isRequired,
};
export default ChoiceInput;
